// NeuroAdapt AI — Backend configuration template
// Copy the real key into a `.env` file at the project root as GEMINI_API_KEY=...
// then run `npm run generate-config` to produce config.js (gitignored, never committed).

export const GEMINI_API_KEY = 'YOUR_GEMINI_API_KEY_HERE';
